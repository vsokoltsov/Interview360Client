machine:
    node:
        version: 7.10
deployment:
  prod:
    branch: master
    commands:
      - ng build --prod --aot
      - aws s3 sync dist s3://interview360manager-client/ --delete
test:
  override:
    - ng test --watch=false
